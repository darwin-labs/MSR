"""
MSR Server Module

This module provides a REST API interface for the MSR framework, allowing clients to 
interact with the framework's capabilities through HTTP requests.
"""

from src.server.app import create_app, run_server